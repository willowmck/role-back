#!/bin/sh
KEYCLOAK_PORT=8080
KEYCLOAK_HOST_PORT=8080
ADMIN=admin 
ADMIN_PASSWORD=admin

docker run -d -p $KEYCLOAK_HOST_PORT:$KEYCLOAK_PORT \
  -e KEYCLOAK_USER=$ADMIN -e KEYCLOAK_PASSWORD=$ADMIN_PASSWORD \
  -e DB_VENDOR=H2 --name keycloak jboss/keycloak 
